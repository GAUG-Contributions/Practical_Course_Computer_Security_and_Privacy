<?xml version="1.0" encoding="utf-8" ?>
<!--
    Xamarin.Forms Shell is a container to reduce the complexity of application development.
    It is recommended to create a Shell application for navigation and performance improvements.
    For more information about Shell, see https://developer.samsung.com/tizen/blog/en-us/2020/03/09/using-xamarinform-shell-on-galaxy-watch
-->
<Shell
    x:Class="SensorFeedback.AppShell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:res="clr-namespace:SensorFeedback.Resources"
    xmlns:views="clr-namespace:SensorFeedback.Views">
    <FlyoutItem Route="Main" Title="{x:Static res:AppResources.MainPageTitle}">
        <ShellSection>
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}"/>
        </ShellSection>
    </FlyoutItem>
    <FlyoutItem Route="FeedbackSettings" Title="{x:Static res:AppResources.FeedbackSettingsPageTitle}">
        <ShellSection>
            <ShellContent ContentTemplate="{DataTemplate views:FeedbackSettingsPage}"/>
        </ShellSection>
    </FlyoutItem>
    <FlyoutItem Route="MonitoringSettings" Title="{x:Static res:AppResources.MonitoringSettingsPageTitle}">
        <ShellSection>
            <ShellContent ContentTemplate="{DataTemplate views:MonitoringSettingsPage}"/>
        </ShellSection>
    </FlyoutItem>
</Shell>
